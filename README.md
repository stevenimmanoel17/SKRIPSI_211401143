# Website Monitoring Penyakit Cabai - Skripsi

Proyek ini menggunakan ESP32-CAM untuk deteksi penyakit dan Dashboard Web berbasis PHP/Python.

## Cara Menjalankan
1. Buat venv: `python -m venv venv`
2. Aktifkan venv: `venv\Scripts\activate`
3. Instal library: `pip install -r requirements.txt`
